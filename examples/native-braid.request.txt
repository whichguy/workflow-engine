Build a durable workflow named native-braid. First create immutable brief.json with the numbers 2, 3, and 5. Then fan out to two independent native-agent steps: one writes only left-count.json with the count, and the other writes only right-sum.json with the sum. Join both pieces of evidence in joined.json. Fan out again to two independent native-agent checks, each writing its own immutable text proof. Join those final checks in a command that writes terminal-proof.json.

The workflow is complete only after every required leaf has accepted evidence. Keep every declared output disjoint and immutable. Each native agent must receive only its current packet, write only its declared output, and report its completion through the workflow script; it must not choose a successor or advance the graph.
